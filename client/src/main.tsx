import React from "react";import{createRoot}from"react-dom/client";import{BrowserRouter,Routes,Route,Navigate}from"react-router-dom";import{QueryClient,QueryClientProvider}from"@tanstack/react-query";import"./index.css";import{Landing}from"./pages/Landing";import{Auth}from"./pages/Auth";import{Layout}from"./components/Layout";import{Dashboard}from"./pages/Dashboard";import{Upload}from"./pages/Upload";import{GeneratorPage}from"./pages/GeneratorPage";
const qc=new QueryClient();const pages=["projects","prd","architecture","database","apis","frontend","backend","testing","documentation","settings"];
createRoot(document.getElementById("root")!).render(<React.StrictMode><QueryClientProvider client={qc}><BrowserRouter><Routes><Route path="/" element={<Landing/>}/><Route path="/login" element={<Auth mode="login"/>}/><Route path="/register" element={<Auth mode="register"/>}/><Route element={<Layout/>}><Route path="dashboard" element={<Dashboard/>}/><Route path="upload" element={<Upload/>}/>{pages.map(p=><Route key={p} path={p} element={<GeneratorPage title={p.replace(/^./,c=>c.toUpperCase())}/>}/>)}</Route><Route path="*" element={<Navigate to="/"/>}/></Routes></BrowserRouter></QueryClientProvider></React.StrictMode>);
