import { useState } from "react";import { Card } from "../components/ui/card";import { Button } from "../components/ui/button";
export function Upload(){const [url,setUrl]=useState("");return <Card><h2 className="text-3xl font-bold">Upload Sketch</h2><label className="mt-6 grid min-h-72 cursor-pointer place-items-center rounded-2xl border border-dashed border-cyan-300/50 bg-white/5"><input type="file" accept=".png,.jpg,.jpeg,.pdf" className="hidden" onChange={e=>{const f=e.target.files?.[0]; if(f&&f.type.startsWith("image/")) setUrl(URL.createObjectURL(f));}}/><span>Drag & drop or click to upload</span></label>{url&&<img src={url} className="mt-4 max-h-96 rounded-xl"/>}<Button className="mt-4">Analyze with AI</Button></Card>}
