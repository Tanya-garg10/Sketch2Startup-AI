services:
  db:
    image: postgres:16
    environment:
      POSTGRES_PASSWORD: postgres
      POSTGRES_DB: sketch2startup
    ports: ["5432:5432"]
  api:
    build: ./server
    env_file: .env
    ports: ["8000:8000"]
    depends_on: [db]
